# One of: openai | gemini | auto   (auto = use OpenAI if key present, else Gemini)
LLM_PROVIDER=auto

# --- OpenAI ---
OPENAI_API_KEY=
MODEL_NAME=gpt-4o-mini

# --- Google Gemini (AI Studio) ---
# Create a free key at https://aistudio.google.com/app/apikey
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash